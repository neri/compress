[package]
name = "compress"
version = "0.1.0"
edition = "2024"

[dependencies]
libm = { version = "0.2" }

[dev-dependencies]
base64 = { version = "0.22" }

# [features]
# default = ["std"]
# std = []

[workspace]
members = [
    "extra/comptest",
]
